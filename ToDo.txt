Create a python script to load rates.csv to google cloud storage bucket.
Create regular getOrCreate functions to load the data and modify them to match airflow

Tests:
1. can call getOrCreate bucket
2. can create a bucket if one does not exists
3. grab bucket if one does exist


